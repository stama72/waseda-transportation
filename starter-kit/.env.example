# ODPT（公共交通オープンデータセンター）APIのアクセストークン。
# https://developer.odpt.org/ で無料登録して取得し、下にコピーする。
# このファイルをコピーして .env を作成すること:  cp .env.example .env
# .env は Git 管理外（コミット禁止）。各自のキーをローカルに設定する。
VITE_ODPT_TOKEN=ここに自分のトークンを貼り付け
